#The below takes input and stores it in "name"
name = input("name: ")
#below we are using the variable "name" and concatenating with an "f string"
#you can also concatenate with just the + sign. The F string just saves time :]
print(f"Hello, {name}!")